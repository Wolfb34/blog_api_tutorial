apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-ui-chart.fullname" . }}
  labels:
    {{- include "blog-ui-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
  selector:
    {{- include "blog-ui-chart.selectorLabels" . | nindent 4 }}
