apiVersion: apps/v1
kind: Deployment
metadata:
    name: blog-postgres-deployment
spec:
    replicas: 1
    selector:
        matchLabels:
            app: blog-postgres
    template:
        metadata:
            labels:
                app: blog-postgres
        spec:
            containers:
                - name: blog-container
                  image: postgres:13.1-alpine
                  imagePullPolicy: "IfNotPresent"
                  ports:
                      - containerPort: 5432
                  envFrom:
                      - configMapRef:
                          name: blog-config
                  env:
                      - name: POSTGRES_PASSWORD
                        valueFrom:
                            secretKeyRef:
                                name: blog-secret
                                key: POSTGRES_PASSWORD
                  volumeMounts:
                      - mountPath: /var/lib/postgresql/data
                        name: postgredb
            volumes:
                - name: postgredb
                  persistentVolumeClaim:
                      claimName: blog-pv-claim
